import styled from 'styled-components'

export const BodyContainer = styled.div`
    background-color: clear;
    padding: 30px var(--ps-body);
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    alignItems: flex-start;
`